## SEO Phase 01 — Audit & Baseline

Auto-generated by the `seo` skill. Read-only audit — no `src/` changes.

### Goal
Capture baseline state across rendering, indexing, schema, performance, link graph, and AI visibility — so every later phase has a number to measure against.

### Headline findings (full detail in `seo/AUDIT.md`)
- **SPA blocks every AI crawler.** All 10 sample URLs return the identical 2413-byte SPA shell to a no-JS UA. Title is generic, H1 empty, body empty, zero JSON-LD. GPTBot/ClaudeBot/PerplexityBot see nothing across the site.
- **Soft-404 plague.** The wildcard rewrite returns 200 + SPA for every non-existent URL, including `/llms.txt`, `/sitemap_index.xml`, and `/totally-fake-page-zzz`. Two suppliers in the sitemap (`materialise-onsite`, `laserhub`) also render "Supplier not found" with JS — likely more.
- **LCP p75 = 18.7s** (Google's "good": 2.5s). Homepage 25.2s.
- **Supplier pages have zero JSON-LD** (no LocalBusiness/Organization/Product/BreadcrumbList) and no canonical.
- **Hreflang `en`/`da`/`x-default` all point to the same URL.** Will be removed in Phase 2 (English-only).
- **Sitemap declares apex URLs but apex 307s to www.** Every sitemap URL redirects on first hit.
- **Brand-name inconsistency:** "SupplyCheck" vs "AMSupplyCheck" vs "Supplycheck" across titles and JSON-LD.
- **AI visibility (SERP proxy): 0/10 queries** return amsupplycheck.com in top 10, including the branded query "amsupplycheck".
- **Gaps**: no GSC/Bing creds (Phase 8), no live-LLM API keys (Phase 8).

### Changes
- `seo/STATE.md` — state machine initialized, Phase 1 in_progress → completed after merge
- `seo/AUDIT.md` — headline findings + prioritized P0/P1/P2/P3 backlog
- `seo/baseline/` — Lighthouse (10 pages), Firecrawl map (484 URLs), render diffs (raw + JS), schema extraction, AI visibility (10 queries), robots/sitemap captures
- `seo/scripts/run-lighthouse.mjs`, `seo/scripts/capture-rendered.mjs` — reproducible audit drivers (used on every re-audit)
- `seo/PR_BODY.md` — this file (regenerated each phase)

### Verification
- [ ] `seo/AUDIT.md` reads cleanly in the GitHub diff viewer
- [ ] All 10 Lighthouse runs succeeded (`seo/baseline/lighthouse-*.json`) — 1 of 10 (`/guides/best-xometry-alternatives`) failed with `NO_FCP`; noted in AUDIT
- [ ] No code changes in `src/` (Phase 1 is read-only by design)

### After merge
On the next `/seo` invocation, the skill detects the merge and starts Phase 2 — the Next.js 15 App Router migration that fixes findings 1–6 above.

🤖 Generated by SupplyCheck `seo` skill
