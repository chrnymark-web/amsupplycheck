import { defineConfig } from "@trigger.dev/sdk";

export default defineConfig({
  project: "proj_zquzqxubqfvhvgeelset",
  runtime: "node",
  logLevel: "log",
  maxDuration: 300,
  dirs: ["./trigger"],
});
